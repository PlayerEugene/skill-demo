import static org.junit.Assert.assertEquals;

import org.junit.Test;

public class SkillDemoTest {
    @Test
    public void testSquare() {
        assertEquals(4, SkillDemo.square(3)); // Initial test fails 4 does not = 9
    }
}
